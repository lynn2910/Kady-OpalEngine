[build]
default-target = "x86_64-unknown-linux-gnu"

[target.aarch64-unknown-linux-musl]
pre-build = [
    # "dpkg --add-architecture $CROSS_DEB_ARCH",
    "apt-get update && apt-get install --assume-yes openssl libssl-dev"
]